# src — 源码目录

> 放项目的可执行源码。按模块拆分子目录，让"某个功能在哪"一目了然。

## 约定
- 一个模块一个子目录，入口集中到 `src/__main__.py` 或 `src/main.py`（后端） / `src/index.js`（前端）。
- 业务逻辑与配置分离：配置从环境变量读（`os.environ` / `process.env`），不要写死绝对路径。
- 公共工具函数放 `src/utils/`，可复用配置放 `src/config/`。
- 保持文件小、职责单一，每个文件顶部写一行用途。
