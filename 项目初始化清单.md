# 项目初始化清单（New Project Kickoff Checklist）

> 开新项目时，按此清单从上到下勾一遍。全部完成 = 项目正式立起来，可以开始写代码。
> 复制本模板到新项目根目录后，把「☐」改成「☑」即可记录进度。

## 阶段 0 · 自省（5 分钟，先想清楚再动手）

- ☐ 一句话定义项目：**为【谁】，在【什么场景】，解决【什么问题】，产出【什么东西】**
  - 我写的是：\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_
- ☐ 成功长什么样（**可验证**的完成标准，不是"做完"）：
  - \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_
- ☐ 愿意投入的上限（时间 / 金钱 / 精力）：
  - \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_

## 阶段 1 · 需求（决定"做什么"，防漂移）

- ☐ 列出本期功能清单（逐条 + 验收标准）
- ☐ 明确本期**不做什么**（范围 = 你决定不做的事）
- ☐ 列出用户角色 / 核心使用场景
- ☐ 标出最大的技术风险 / 不确定点（需要提前验证的假设）

## 阶段 2 · 设计与选型

- ☐ 画一张架构/模块草图（模块怎么交互、数据存哪）
- ☐ 选定技术栈（优先你熟、生态成熟、能长期维护的）
- ☐ 规划目录结构（照本模板 `src/ tests/ docs/ outputs/`）
- ☐ 对最有风险的技术点先做最小原型（spike）验证

## 阶段 3 · 初始化（一鼓作气，别省）

- ☐ `git init` 并做**第一个 checkpoint**（从此刻起每一步可回退）
- ☐ 填好 `README.md`（运行方式 / 结构 / 配置 / 技术栈）
- ☐ 建好 `.gitignore`（排除依赖、构建产物、`.env`）
- ☐ 复制 `.env.example` 为 `.env`，填真实值，确认 `.env` 不进 git

## 阶段 4 · 开发

- ☐ 按功能清单**一条一条**做：实现 → 自测 → 提交
- ☐ 每次提交写清楚"做了什么 + 为什么"（feat / fix / docs / refactor / test 前缀）
- ☐ 每完成一个功能，按它的验收标准自测一遍

## 阶段 5 · 测试

- ☐ 每条功能按验收标准走一遍（功能测试）
- ☐ 关键边界条件：空输入 / 非法输入 / 超长输入 / 并发 / 断网
- ☐ 用**真实数据 / 真实环境**跑一次，而不只是理想测试数据

## 阶段 6 · 交付

- ☐ README 是否完整（安装 / 运行 / 结构 / 配置 / 技术栈 / 已知限制）
- ☐ **可迁移性自查**：拷到干净目录按 README 走一遍，能否直接跑？
  - 有没有写死本机绝对路径？
  - 有没有依赖项目外分发的工具 / 凭据？（已集中到 `.env` 并写进 README）
  - 依赖清单是否完整（requirements.txt / package.json）？
- ☐ 打包时**绝不含 `.env`、登录态、临时文件**
- ☐ 最终干净提交（或打 tag 标版本）

## 阶段 7 · 维护

- ☐ 记录发现的 bug / 新需求（放 `ISSUES.md`，别只记脑子）
- ☐ 决定哪些下期做、哪些永远不做（用阶段 1 的范围拒绝）
- ☐ 每次改动都保持提交记录，文档保持最新

---

## 生命周期总览（贴在墙上）

| 阶段 | 关键动作 | 为什么 |
| --- | --- | --- |
| 0 自省 | 目标 / 成功标准 / 投入上限 | 锚定取舍，防烂尾 |
| 1 需求 | 一句话定义 + 完成标准 + 范围 + 功能清单 | 决定做什么，防漂移 |
| 2 设计 | 架构草图 + 选型 + 目录规划 + 原型验证 | 骨架对了后面才顺，低成本排风险 |
| 3 初始化 | git + README + .gitignore + .env | 地基 + 可迁移性 + 密钥安全 |
| 4 开发 | 小步迭代 + 有意义提交 + 逐步自测 | 可回退、可定位、及时修 |
| 5 测试 | 功能 + 边界 + 真实场景 | 暴露没想到的情况，而非证明能用 |
| 6 交付 | README + 迁移自查 + 打包 + 最终提交 | 让别人(和未来的你)能跑起来 |
| 7 维护 | 记录 bug / 需求 + 保持文档最新 | 让项目能持续活下去 |
