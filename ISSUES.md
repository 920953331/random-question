# Issues / 需求记录

> 记录开发与使用过程中的 bug、新需求、改进想法。**别只记在脑子里。**
> 配合生命周期「阶段7 维护」使用：定期整理，决定哪些下期做、哪些放弃。
> 一个项目一份，每次改动都留痕。

## 使用说明

- **一条记录一行，发现即登记**，不拖到"忙完再记"。
- 类型：
  - `bug` —— 已经出问题（功能/兼容/报错）
  - `需求` —— 新功能或新能力
  - `改进` —— 现有体验/性能优化
  - `想法` —— 还拿不准，先登记待评估
- 严重度：`致命` / `高` / `中` / `低` / `建议`。
- 状态：`待办` / `进行中` / `已完成` / `已放弃`。
- 「下期做还是不做」的判断依据，集中写在文末「决策记录」。

---

## 问题与需求登记（按时间倒序，新的在上）

| 编号 | 日期 | 类型 | 标题 / 描述 | 严重度 | 状态 | 负责人 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| #001 | YYYY-MM-DD |  |  |  |  |  |  |
| #002 | YYYY-MM-DD |  |  |  |  |  |  |
| #003 | YYYY-MM-DD |  |  |  |  |  |  |

---

## 决策记录（哪个做 / 哪个不做）

| 编号 | 决策 | 理由 | 日期 |
| --- | --- | --- | --- |
|  |  |  |  |
|  |  |  |  |

### 本期明确不做（范围，参考 docs/项目规划.md）

- 判分 / 对错判断
- 答案自动展示（题库已预留 a 字段）
- 用户账号 / 多用户
- 后端进程 / 数据库
- 记忆曲线 / 复习计划

### 部署方式（已决策）

- 方案二：云静态托管（首选 GitHub Pages），把 `outputs/出题工具.html` 上传即可，免费、无需云服务器。
- 局域网临时用：`python -m http.server 8000 --directory outputs`，手机连同一 WiFi 用电脑 IP 访问。

> 范围是救命的。写下"本期不做 X"，你才能对突然冒出来的想法说"下期再做"，而不是被它牵着跑。
